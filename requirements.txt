# Core ML
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0
datasets>=2.19.0
huggingface_hub>=0.22.0

# Experiment utilities
scikit-learn>=1.4.0
numpy>=1.26.0
scipy>=1.13.0

# Data / I/O
pyyaml>=6.0
tqdm>=4.66.0
h5py>=3.11.0        # HDF5 support (recommended on HLR for large datasets)

# Optional: 8-bit quantization (requires CUDA/ROCm bitsandbytes build)
# bitsandbytes>=0.43.0

# Visualisation (optional, for local analysis)
matplotlib>=3.8.0
seaborn>=0.13.0
